{% extends 'base.html.twig' %}

{% block title %}Hello CdAdminController!{% endblock %}

{% block body %}
<div class="container d-flex justify-content-center align-items-center mt-5">
    <div class="w-75">
        <h1>{{ controller_name }}! ✅</h1>

        <table class="table mt-5">
            <thead>
                <tr>
                    <th>Id</th>
                    <th>Nom</th>
                    <th>Prénom</th>
                    <th>Adresse</th>
                    <th>CP</th>
                    <th>Ville</th>
                    <th>Référence Commandes</th>
                </tr>
            </thead>
            <tbody>
                {% for user in users %}
                <tr>
                    <td>{{user.id}}</td>
                    <td>{{user.lastname}}</td>
                    <td>{{user.firstname}}</td>
                    <td>{{user.address}}</td>
                    <td>{{user.zipcode}}</td>
                    <td>{{user.city}}</td>
                    {% for oder in user.orders %}
                    <td>{{order.id}}</td>
                    {% endfor %}
                </tr>
                {% endfor %}

            </tbody>
        </table>

    </div>
</div>
{% endblock %}
